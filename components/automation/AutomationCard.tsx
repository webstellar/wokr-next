import { jobData } from "@/types/types";

interface jobProps {
  data: jobData;
}

const AutomationCard: React.FC<jobProps> = ({ data }) => {
  return <div className="rounded-md border-2 grid ">
    
  </div>;
};

export default AutomationCard;
 